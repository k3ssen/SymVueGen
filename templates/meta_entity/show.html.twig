{% extends 'base.html.twig' %}

{% block title %}MetaEntity{% endblock %}

{% block body %}
    <h1>MetaEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ meta_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ meta_entity.name }}</td>
            </tr>
            <tr>
                <th>Namespace</th>
                <td>{{ meta_entity.namespace }}</td>
            </tr>
            <tr>
                <th>Uses</th>
                <td>{{ meta_entity.uses ? meta_entity.uses|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('meta_entity_index') }}">back to list</a>

    <a href="{{ path('meta_entity_edit', {'id': meta_entity.id}) }}">edit</a>

    {{ include('meta_entity/_delete_form.html.twig') }}
{% endblock %}
